{
  "name": "notes-app-backend",
  "version": "1.0.0",
  "description": "Backend part for the notes app",
  "main": "build/index.js",
  "scripts": {
    "start": "nodemon --exec babel-node src/app.js",
    "build": "babel ./src --out-dir build",
    "build:remove": "rm -rf build/",
    "minify": "minify ./src --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "backend",
    "mern",
    "nodejs",
    "express"
  ],
  "author": "Petar S.",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.8.4",
    "@babel/node": "^7.8.4",
    "@babel/preset-env": "^7.8.4",
    "nodemon": "^2.0.2"
  }
}
